const Form = load.class('form');

class {% className %} extends Form {

    getSchema() {
        return {% formProperties %};
    }

    buildComponents() {
        this.buildComponentsFromSchemaList({% childrenSchema %})
    }

}

module.exports = {% className %};
